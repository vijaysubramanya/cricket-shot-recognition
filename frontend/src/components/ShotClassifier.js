import React from 'react';

const ShotClassifier = () => {
  // This component is currently a placeholder
  // It can be used for additional shot classification features in the future
  return null;
};

export default ShotClassifier; 